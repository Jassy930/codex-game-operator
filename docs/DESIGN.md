# Design

核心循环：点击获得资源 -> 购买升级 -> 提升产出 -> 追逐下一目标。

M0 设计约束：

- 第一次升级成本为 10，确保短时间内出现明确反馈。
- 主操作区必须包含随静态构建发布的本地视觉资产，表现星核和关键升级装置；视觉资产只改变展示，不改变存档、数值、升级价格、星图航段、航线策略、航线指令或反馈入口。
- 主点火按钮必须提供即时视觉反馈：pointerdown 按住阶段先根据当前落点触发即时反冲，并让星核本体、蓄能外弧和中心标签进入按住压光；普通点击触发短脉冲、按压反冲、外层轮廓命中亮闪、中心标签命中亮脉冲、粒子层、蓄能轨命中闪烁、点击落点闪光、落点涟漪、落点火花束和本次 `+X` 收益浮层，收益浮层必须跟随实际点击落点附近出现并用边界约束避免贴边溢出；接近过载时显示蓄能前兆，过载触发时显示更强冲击波、按压反冲、过载外层轮廓亮闪、过载中心标签闪爆、过载蓄能轨余辉、过载落点闪光、过载涟漪、过载火花束和过载收益浮层，并让顶部过载奖励读数短促亮闪；按住即时反冲、按压反冲、落点闪光、涟漪、火花束与收益浮层坐标只从指针/点击事件坐标派生，键盘触发必须回退到按钮中心；外层轮廓命中亮闪只复用现有 `is-pulsing` / `is-overload-impact` 短时展示态并叠加到 `::before` 外层轮廓；中心标签命中亮脉冲只复用现有 `is-pulsing` 短时展示态且不得覆盖 `is-overload-impact`；过载中心标签闪爆只复用现有 `is-overload-impact` 短时展示态；蓄能轨命中闪烁只复用现有 `is-pulsing` / `is-overload-impact` 展示态；过载奖励读数亮闪只复用 `animateCore` 的过载命中状态并不得改变过载奖励数值；按住压光只复用 `is-pressing` 展示态；反馈只改变展示，不改变存档、点击收益、连击窗口、过载奖励、升级价格、星图航段、航线策略、航线指令或反馈入口。
- 主点火按钮必须让星核本体随连击进度产生视觉变化：由现有 `combo.progress` 派生 `--core-vein-opacity` 和 `--core-vein-dash-offset`，让星核蓄能裂纹逐步显现，过载前一击脉动，过载命中爆发；裂纹只改变展示，不新增存档字段，不改变点击收益、连击窗口、过载奖励、升级价格、星图航段、航线策略、航线指令或反馈入口。
- 主点火按钮必须提供蓄能外弧：由现有 `combo.progress` 派生 `--core-surge-opacity`、`--core-surge-scale` 和 `--core-surge-speed`，让外层旋转能量弧随连击推进增强、放大并加快，过载前一击和过载命中时加强状态；外弧只改变展示，不新增存档字段，不改变点击收益、连击窗口、过载奖励、升级价格、星图航段、航线策略、航线指令或反馈入口。
- 主点火按钮必须提供可关闭的本地音效反馈：默认开启，普通点火与过载点火使用不同 Web Audio 短音；偏好只能写入独立 localStorage key，不进入游戏进度存档，不改变点击收益、连击窗口、过载奖励、升级价格、星图航段、航线策略、航线指令或反馈入口。
- 主点火按钮必须提供可关闭的移动端触感反馈：默认开启，普通点火使用短震动，过载点火使用更强节奏；偏好只能写入独立 localStorage key，不进入游戏进度存档，触感调用必须通过浏览器 `navigator.vibrate` 能力检测，不改变点击收益、连击窗口、过载奖励、升级价格、星图航段、航线策略、航线指令或反馈入口。
- 主点火按钮必须提供环形蓄能轨：由现有连击进度派生 `--core-charge-angle`，普通连击逐步点亮，普通点击命中时播放 `coreChargeRingHit` 短闪，过载命中时播放 `coreChargeRingOverloadHit` 更强余辉，过载前兆和过载命中时高亮；蓄能轨只改变展示，不新增存档字段，不改变点击收益、连击窗口、过载奖励、升级价格、星图航段、航线策略、航线指令或反馈入口。
- 主点火按钮必须提供阶段光环：由现有连击进度派生 `--core-stage-angle`，3/8 后预热、6/8 后升温、过载前一击脉动，过载命中时爆发；阶段光环只改变展示，不新增存档字段，不改变点击收益、连击窗口、过载奖励、升级价格、星图航段、航线策略、航线指令或反馈入口。
- 主点火按钮必须提供过载倒计时徽标：由现有 `getComboStatus()` 派生剩余过载次数，普通点火命中时复用 `.core-button.is-pulsing:not(.is-overload-impact)` 播放 `coreBadgeStepHit` 短跳闪，过载前一击高亮，过载命中时显示爆发态；倒计时徽标只改变展示，不新增存档字段，不改变点击收益、连击窗口、过载奖励、升级价格、星图航段、航线策略、航线指令或反馈入口。
- 主点火按钮必须显示 8 格连击轨、下一击奖励预告和连击读数命中反馈，直接展示当前连击进度、下一次点火收益和临近过载的奖励；连击轨必须从现有 `combo.progress` 派生连续填充条，每次点火后必须复用点火按钮 `is-pulsing` / `is-overload-impact` 短时状态让连续填充条播放普通/过载两套命中光扫，并从现有 `comboStatus.step` 派生本次命中点跳闪，过载前一击和过载命中时加强填充状态，第 8 格过载命中时本次命中点使用更强跳闪；下一击奖励预告必须复用点火按钮相邻的 `is-pulsing` / `is-overload-impact` 短时状态，在普通命中和过载命中后播放不同强度的跳闪；下方 `连击` / `稳定` 读数必须复用同一按钮短时状态，普通命中时让连击读数跳闪，过载命中时让连击和过载状态读数播放更强跳闪；预告、读数跳闪、填充条光扫、填充条和命中点跳闪只从现有点击产能、过载奖励和连击状态推导，不新增存档字段，不改变点击收益、连击窗口、过载奖励、升级价格、星图航段、航线策略、航线指令或反馈入口。
- 第八次连续点击触发过载奖励，界面必须显示过载进度和剩余点击数，增强早期节奏。
- 过载奖励必须有可见成长线：星核谐振器提升过载奖励，顶部状态区必须显示当前过载奖励。
- 点击、过载和购买升级后必须显示即时行动结果，说明本次收益或产能变化。
- 目标条按“首次升级 -> 自动产能 -> 累计能量 -> 稳定器”推进，并必须显示进度、剩余量、购买型目标的关键升级能量缺口和完成状态。
- 购买型目标对应的升级卡片必须显示“目标推荐”并高亮，降低目标条和升级区之间的匹配成本。
- 升级卡片必须显示按升级类型区分的固定尺寸图标，覆盖点击、自动、总产能和过载成长线；图标只改变展示，不改变存档、升级价格、产能公式、星图航段、航线策略、航线指令或反馈入口。
- 100K 以后必须出现可见的中后段项目目标；下一个未完成项目必须进入主目标条，项目目标要显示进度、剩余量、完成状态和奖励，主目标条也要直接显示当前项目奖励，项目完成提示必须确认已获得的奖励，并对有效点击/自动/总产能或过载奖励产生可测试影响。
- 星图主目标、星图总览和星图项目列表必须显示航段序号，让玩家知道当前项目位于完整星图路线的第几段。
- 点亮星图后的星图项目必须包含谐振校准目标，让中后段内容不只围绕常规产能倍率循环。
- 累计 100K 能量后必须解锁星图航线策略，让玩家能在点击、自动和默认节奏之间主动切换；旧存档默认策略不得改变既有产能。
- 航线策略按钮必须提供固定尺寸视觉徽记，覆盖均衡航线、点火优先和巡航优先三种策略；徽记只改变展示，不新增存档字段，不改变航线策略倍率、解锁条件、专精航段、航线指令、星图路线、升级价格或反馈入口。
- 累计 100K 能量后必须出现航线指令，让玩家在点火、等待自动产能和购买升级之外有主动选择；首批指令必须分别对应点击、自动和过载收益，并用独立冷却限制爆发频率。航线指令必须支持 90 秒“航线连携”：轮换不同指令给下一次指令 +12%/+24% 封顶加成，完成 3/3 轮换时额外获得基础指令收益 18% 的轮换目标奖励；如果 3/3 第三步收束到当前航线策略契合指令，还必须额外获得基础指令收益 12% 的策略终结奖励；重复同类或超时重置；旧存档必须自动补齐指令冷却和连携字段；指令不得改变升级价格、星图航段顺序、项目完成判定、项目奖励或筛选规则。
- 航线指令按钮必须提供固定尺寸视觉徽记，覆盖点火齐射、巡航回收和谐振脉冲三种主动指令；徽记只改变展示，不新增存档字段，不改变指令收益、冷却、连携窗口、远航调度、星图路线、升级价格或反馈入口。
- 航线指令区必须包含项目内本地视觉插画，表现点火齐射、巡航回收、谐振脉冲和 3 步轮换轨；插画只改变展示，不新增存档字段，不改变指令收益、冷却、连携窗口、远航调度、星图路线、升级价格或反馈入口。
- 航线指令徽记必须提供状态灯：锁定态显示默认弱状态，可执行态显示可用状态，冷却中显示冷却状态，推荐态可加强状态边框；状态灯只从 `ready`、`cooling`、`recommended` 派生，不新增存档字段，不改变指令收益、冷却、连携窗口、远航调度、星图路线、升级价格或反馈入口。
- 航线指令按钮必须压缩徽标和收益明细的默认可见文本：运行期每个按钮最多直接显示 3 个文字徽标，其余用 `+N` 提示；预计收益行显示“预计 +X 能量 · N 项明细”，完整 `previewText` 必须保留到 `title` / `aria-label`。该压缩只改变展示层，不新增存档字段，不改变指令收益、冷却、连携窗口、远航调度、星图路线、升级价格或反馈入口。
- 航线策略必须和航线指令形成主动选择关系：均衡航线契合谐振脉冲，点火优先契合点火齐射，巡航优先契合巡航回收；匹配当前策略的指令获得 10% 策略契合加成，并在预计收益、执行反馈、按钮徽标和本地 `directive` 事件中可见。策略契合不得新增存档字段，不得改变指令冷却、连携窗口、轮换目标奖励、星图航段或升级价格。
- 航线指令必须显示可执行的轮换目标：锁定时提示 100K 解锁；解锁后显示 0/3 起步目标，并在 0/3 时把非契合指令标为“收束起手”、把当前策略契合指令留给第三步策略终结，推荐起手仍在冷却时显示“等待起手”；1/3 且上一指令不是当前策略契合指令时，必须继续推荐另一条非契合指令并显示“收束续航/等待续航”，避免第二步提前消耗第三步策略终结指令；连携窗口内显示当前指令、轮换进度、剩余窗口和下一步建议；接近 3/3 时必须提示轮换目标奖励，且当下一步可收束到当前策略契合指令时提示策略终结奖励；推荐的下一步指令必须在按钮层显示“轮换推荐”，推荐但仍冷却的指令显示“等待轮换”，可触发策略终结的推荐指令必须额外显示“策略终结”徽标；该提示只从现有冷却与连携字段推导，不新增存档字段。
- 航线指令轮换目标必须提供 3 格视觉轨：锁定态显示 100K 解锁；解锁后按 `getDirectivePlan.progress` / `target` / `nextDirectiveIds` 显示 1/3 到 3/3 的已完成、下一步和待推进状态，下一步格必须显示当前推荐指令名；完成 3/3 后显示轮换完成。视觉轨只改变展示，不新增存档字段，不改变指令收益、冷却、连携窗口、远航调度或星图项目。
- 航线指令支持说明必须默认摘要化：`directivePlan` 显示 `summaryText`，航线委托显示进度/完成短句，远航调度显示当前航段和目标指令短句；完整说明必须保留到 `title` / `aria-label`，并用单行省略避免小屏文字墙。摘要化只改变展示，不新增存档字段，不改变指令收益、冷却、连携窗口、远航调度或星图项目。
- 航线指令推荐预案必须有即时收益反馈：执行当前 `getDirectivePlan.nextDirectiveIds` 推荐的指令时，额外获得有效基础指令收益 6% 的“预案执行”奖励；预计收益、执行反馈和本地 `directive` 事件必须记录 `planReward` / `planBonusRate`；按钮徽标区必须直接显示 `planRewardText`，让推荐指令的额外收益可扫视。预案执行不得新增存档字段，不得改变升级价格、星图航段顺序、项目完成判定、项目奖励、指令基础收益、冷却、连携窗口、轮换目标奖励、策略契合、策略终结、指令熟练、满层回响或筛选规则。
- 航线指令必须提供独立短期任务“航线委托”：累计 100K 能量后显示 0/3 到 3/3 推荐轮换委托，并用固定进度条和 `role="meter"` 暴露当前进度；当玩家按推荐预案把轮换从 2/3 完成到 3/3 时，额外获得有效基础指令收益 8% 的“委托完成”奖励；按钮徽标、预计收益、执行反馈和本地 `directive` 事件必须记录 `taskReward` / `taskRewardRate`；3/3 后必须显示委托已完成，并提示重置或超时后开启下一轮委托。航线委托不得新增存档字段，不得改变升级价格、星图航段顺序、项目完成判定、项目奖励、指令基础收益、冷却、连携窗口、轮换目标奖励、预案执行、策略契合、策略终结、指令熟练、满层回响或筛选规则。
- 20M 后必须提供“远航调度”，让当前未完成星图航段改变目标指令：点击奖励航段对应点火齐射，自动奖励航段对应巡航回收，过载奖励航段对应谐振脉冲，总产能奖励航段对应当前航线策略契合指令；目标指令必须接管指令轮换的下一步推荐并显示“调度目标/等待调度”，当调度目标与常规收束起手冲突时优先服从当前航段调度；执行目标指令必须获得有效基础指令收益 14% 的“调度校准”奖励，目标指令冷却必须按 0.7 倍生效并显示“调度冷却 -30%”，且本轮指令连携窗口必须从 90 秒延长到 120 秒并显示“调度接力 +30 秒”；目标指令后的 1/3 阶段必须把可用非目标指令标为“远航续航/等待续航”，允许任意非目标指令触发有效基础指令收益 8% 的远航续航奖励，同时按目标指令指定一个协同续航指令，该按钮必须单独显示“远航协同/等待协同”，执行该指令额外获得有效基础指令收益 5% 的“远航协同”奖励，并额外获得有效基础指令收益 3% 的“协同补给”当前能量补给；协同补给必须只增加当前能量，不增加累计能量；执行协同或绕行分支必须触发“回航校准”，刷新当前目标指令冷却并在按钮徽标、预计收益、执行反馈、本地 `directive` 事件和反馈快照中记录 `dispatchRefreshDirectiveId` / `dispatchRefreshDirectiveName` / `dispatchRefreshText`；执行指定协同续航后回到当前航段目标指令时，必须显示“协同回航/等待协同”，并提示触发远航闭环与远航突破；目标后的另一个非目标指令必须显示“远航绕行/等待绕行”，执行时额外获得有效基础指令收益 4% 的“远航绕行”奖励，并触发“绕行投送”：消耗少量当前能量，按 150% 投送量推进累计能量；绕行后下一步回到当前航段目标指令时显示“绕行回航/等待绕行”、额外按当前能量航段剩余量 0.03% 注入“绕行突破”；当 3/3 指令轮换回到当前航段目标指令时，必须额外获得有效基础指令收益 16% 的“远航闭环”奖励，并按当前能量航段剩余量的 0.05% 获得“远航突破”能量注入，同时触发“远航整备”刷新协同续航指令冷却，完成态下一步必须优先推荐“整备续航/等待整备”；执行该整备推荐的协同续航指令必须额外获得有效基础指令收益 7% 的“整备续航”奖励；执行整备续航后回到当前航段目标指令时，必须显示“整备回航/等待回航”，并额外获得有效基础指令收益 6% 的“整备回航”奖励；按钮徽标、预计收益、执行反馈和本地 `directive` 事件必须记录远航协同补给、远航绕行、绕行投送消耗、绕行投送累计推进、绕行突破、远航突破、远航整备、整备续航和整备回航字段。远航调度条必须同时显示当前航段进度和 0/3 到 3/3 的闭环进度，闭环进度必须使用 `role="meter"` 暴露，并在 1/3 时提示优先协同续航指令触发远航协同与协同补给，或切另一非目标指令触发远航绕行并进行绕行投送，在 2/3 时提示协同回航到目标触发远航闭环与远航突破，或在绕行路线回到目标追加绕行突破，在 3/3 时提示远航整备优先触发整备续航，整备续航后提示回到目标触发整备回航；按钮徽标、预计收益、执行反馈、本地 `directive` 事件和反馈快照中必须可见相关状态。远航调度只允许新增 `farRouteLastBranchDirectiveId` 和短期待结算字段 `farRouteBranchRotationDirectiveId`，不得改变升级价格、星图航段顺序、项目完成判定、项目奖励、航线策略、指令基础收益、基础连携倍率、轮换目标奖励、预案执行、航线委托、策略契合、策略终结、指令熟练或满层回响。
- 远航绕行路线必须提供“绕行整备”：选择绕行并回到目标完成闭环时，必须刷新刚才的绕行指令冷却；完成态下一步必须优先推荐“绕行整备/等待绕行整备”，执行该绕行指令必须额外获得有效基础指令收益 5%，并在按钮徽标、预计收益、执行反馈、本地 `directive` 事件和反馈快照中记录 `dispatchDetourPrepReward` / `dispatchDetourPrepRewardRate` / `dispatchDetourPrepRewardText`。
- 远航调度必须显示当前分支态势：active 状态下由现有 `directiveChain`、目标指令、协同指令和 `farRouteLastBranchDirectiveId` 派生 `branchKind` / `branchText`，覆盖待选择、协同、绕行、协同整备和绕行整备；主操作区必须渲染 `far-dispatch-branch`，星图总览和反馈快照必须包含分支态势。该展示不新增存档字段，不改变收益、冷却、连携窗口、星图航段、项目奖励、反馈入口或部署链路。
- 远航调度必须提供“分支改道”：目标指令后的分支选择阶段，如果当前选择的协同/绕行分支不同于上一轮远航分支，必须额外获得有效基础指令收益 6%；上一轮协同后改走绕行、上一轮绕行后改走协同触发，重复同一分支不触发。该机制复用 `farRouteLastBranchDirectiveId`，不得新增存档字段；按钮徽标、预计收益、执行反馈、本地 `directive` 事件和反馈快照必须记录 `dispatchBranchShiftReward` / `dispatchBranchShiftRewardRate` / `dispatchBranchShiftRewardText`。
- 远航调度必须提供“路线稳航”：目标指令后的分支选择阶段，如果当前选择的协同/绕行分支等于上一轮远航分支，必须额外获得有效基础指令收益 4%；上一轮协同后续走协同、上一轮绕行后续走绕行触发，改走另一分支不触发。该机制复用 `farRouteLastBranchDirectiveId`，不得新增存档字段；按钮徽标、预计收益、执行反馈、本地 `directive` 事件和反馈快照必须记录 `dispatchBranchStabilityReward` / `dispatchBranchStabilityRewardRate` / `dispatchBranchStabilityRewardText`。
- 远航调度必须提供“轮替闭环”：目标指令后的分支选择阶段触发分支改道时，必须把本轮分支写入 `farRouteBranchRotationDirectiveId`；随后回到当前航段目标指令完成 3/3 时，必须额外获得有效基础指令收益 9%，并清空该待结算字段。按钮徽标、预计收益、执行反馈、本地 `directive` 事件和反馈快照必须记录 `dispatchBranchRotationReward` / `dispatchBranchRotationRewardRate` / `dispatchBranchRotationRewardText`；重复同一分支或没有待结算轮替分支时不得触发。
- 远航调度条必须提供分支选择条：active 状态下由当前目标指令、协同续航指令、绕行备选指令、当前分支态势和 `farRouteLastBranchDirectiveId` 派生 `branchChoices` / `branchChoiceText`；主操作区必须渲染 `far-dispatch-branch-choices`，并用两个固定槽展示协同与绕行路线、资源取向、推荐原因、后续回航预告、上轮路线、当前路线、路线稳航和可改道状态。协同路线必须显示“后续协同回航触发闭环与远航突破”，绕行路线必须显示“后续绕行回航触发闭环与绕行突破”；只有当前航段契合路线显示 `choice.reasonText`。每条路线必须派生 `choice.decisionText` 并渲染 `far-dispatch-branch-choice-decision`，展示当前航段首推、推荐稳航、推荐改道、备选建档、备选稳航、备选改道、本轮已选或本轮已完成；每条路线必须派生 `choice.payoffText` 并渲染 `far-dispatch-branch-choice-payoff`，展示本步合计收益和回目标闭环收益；每条路线也必须派生 `choice.objectiveText` 并渲染 `far-dispatch-branch-choice-objective`，展示当前选择是建档、续走稳航、改道轮替、本轮回航确认或整备接回目标；每条路线还必须派生 `choice.followupText` 并渲染 `far-dispatch-branch-choice-followup`，展示当前应先执行目标、执行该分支后回目标、回目标闭环或整备回航；这些提示只从现有奖励常量、航段契合、上轮路线、当前分支状态、目标指令、分支指令和轮替状态派生，不新增收益或存档字段。该展示不新增存档字段，不改变分支改道收益、路线稳航收益、远航协同、协同补给、远航绕行、绕行投送、冷却、连携窗口、星图航段、项目奖励、反馈入口或部署链路。
- 远航路线判断必须提供视觉状态：每条 `branchChoices` 必须从现有判断条件派生 `decisionKind`，覆盖推荐、推荐稳航、推荐改道、备选、备选稳航、备选改道、本轮已选和本轮已完成；主操作区分支卡片与 `far-dispatch-branch-choice-decision` 必须渲染 `is-decision-*` 状态 class。该状态只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航路线判断必须提供首行徽标：每条 `branchChoices` 必须从 `decisionKind` 派生 `decisionBadgeText`，覆盖首推、稳航、改道、建档、备选稳航、备选改道、已选和已完成；主操作区分支卡片必须渲染 `far-dispatch-branch-choice-head` 与 `far-dispatch-branch-choice-badge is-*`，并把短徽标纳入 `branchChoiceText` 和卡片悬停说明。该徽标只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航路线分支卡片必须提供路线微图：每条 `branchChoices` 必须渲染 `far-dispatch-branch-choice-route`，包含起点、分支点和回目标点；协同路线以直线轨表示，绕行路线以偏移节点表示，改道/推荐态可通过既有 class 高亮。路线微图必须 `aria-hidden="true"`，语义继续由 `branchChoiceText`、卡片标题和路线文本承载；该微图只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航路线微图必须提供资源取向符号：每条 `branchChoices` 必须派生 `routeResourceKind` / `routeResourceText`，协同为 `current / 保留当前`，绕行为 `progress / 投送累计`；主操作区路线微图必须渲染 `far-dispatch-branch-choice-route-resource is-current/is-progress`，用图形区分保留当前资源和投送累计航段。资源取向符号必须 `aria-hidden="true"`，语义继续由 `caption`、`branchChoiceText`、卡片标题、收益对照和路线目标承载；该符号只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航路线微图必须提供路线状态标记：每条 `branchChoices` 必须派生 `routeMarkerKind` / `routeMarkerText`，覆盖推荐、备选、本轮、推荐上轮、推荐改道、上轮和改道；主操作区路线微图必须渲染 `far-dispatch-branch-choice-route-marker is-*`，把当前路线状态压到路线节点附近。路线微图标记必须 `aria-hidden="true"`，语义继续由 `caption`、`branchChoiceText`、卡片标题、路线判断、路线目标和收益对照承载；该标记只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航路线微图必须提供步骤高亮：每条 `branchChoices` 必须派生 `routeNodeStates`，为起点、分支点和回目标点标记 `done`、`next` 或 `waiting`；主操作区路线微图节点必须渲染 `far-dispatch-branch-choice-route-node is-done/is-next/is-waiting`，让 0/3 起手、1/3 选分支、2/3 回目标和完成态可被扫视。步骤高亮必须 `aria-hidden="true"`，语义继续由路线步骤、路线下一步、按钮路线标记和 `branchChoiceText` 承载；该高亮只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航路线微图必须提供步骤序号：每条 `branchChoices` 必须派生 `routeStepLabels`，为起点、分支点和回目标点标记 `1 / 2 / 3`；主操作区路线微图节点必须把序号写入 `data-step-label` 并通过节点样式显示。步骤序号必须 `aria-hidden="true"`，语义继续由路线步骤、路线下一步、按钮路线标记和 `branchChoiceText` 承载；该序号只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航路线微图必须提供方向箭头：路线轨道必须通过 `.far-dispatch-branch-choice-route-line::before` 和 `::after` 显示从起点到分支、从分支到回目标的两个推进方向箭头；协同和绕行路线必须使用各自轨道色。方向箭头必须 `aria-hidden="true"`，语义继续由路线步骤、路线下一步、按钮路线标记和 `branchChoiceText` 承载；该箭头只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航路线微图必须提供推进填充：每条 `branchChoices` 必须从 `routeNodeStates` 派生 `routeProgressPercent`，主操作区路线微图必须把该百分比写入 `--branch-route-progress`，并通过 `--branch-route-line-fill` / `--branch-route-line-track` 渲染轨道填充。0/3 起手为 0%，1/3 分支选择为 50%，当前回航或完成路线为 100%，未选路线保持 0%。推进填充必须 `aria-hidden="true"`，语义继续由路线步骤、路线下一步、按钮路线标记、路线反馈和 `branchChoiceText` 承载；该填充只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航路线微图必须提供当前步短标：每条 `branchChoices` 必须从 `routeNodeStates` 派生 `routePhaseKind` / `routePhaseText`，覆盖 `0/3 起手`、`1/3 分支`、`2/3 回航`、`3/3 完成` 和 `未选`；主操作区路线微图必须渲染 `far-dispatch-branch-choice-route-phase is-*`，并把短标语义纳入 `branchChoiceText` 与卡片标题。当前步短标只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航路线微图必须提供收益短标：每条 `branchChoices` 必须从路线类型派生 `routeRewardLabels` / `routeRewardText`，协同路线为 `校准 / 补给 / 闭环`，绕行路线为 `校准 / 投送 / 闭环`；主操作区路线微图必须渲染 `far-dispatch-branch-choice-route-reward is-start/is-branch/is-return`，并把收益短标语义纳入 `branchChoiceText` 与卡片标题。收益短标只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航路线微图必须提供资源流向短标：每条 `branchChoices` 必须从路线类型派生 `routeFlowKind` / `routeFlowText`，协同路线为 `current / 当前+`，绕行路线为 `progress / 当前->累计`；主操作区路线微图必须渲染 `far-dispatch-branch-choice-route-flow is-current/is-progress`。资源流向短标只改变展示，不新增收益、存档字段、远端指标或部署链路，完整语义继续由资源取向符号、`caption`、`branchChoiceText`、卡片标题、收益对照和展开明细承载。
- 远航路线分支卡片必须提供代价短标：每条 `branchChoices` 必须从路线类型派生 `routeCostKind` / `routeCostText`，协同路线为 `safe / 无消耗`，绕行路线为 `spend / 消耗当前`；主操作区分支卡片首行必须渲染 `far-dispatch-branch-choice-cost is-safe/is-spend`，并把代价短标语义纳入 `branchChoiceText` 与卡片标题。代价短标只改变展示，不新增收益、存档字段、远端指标或部署链路，完整语义继续由资源流向短标、资源取向符号、收益对照和展开明细承载。
- 远航路线分支卡片必须提供取向短标：每条 `branchChoices` 必须从路线类型派生 `routeIntentKind` / `routeIntentText`，协同路线为 `preserve / 保当前`，绕行路线为 `advance / 推累计`；主操作区分支卡片首行必须渲染 `far-dispatch-branch-choice-intent is-preserve/is-advance`，并把取向短标语义纳入 `branchChoiceText` 与卡片标题。取向短标只改变展示，不新增收益、存档字段、远端指标或部署链路，完整语义继续由资源流向短标、代价短标、资源取向符号、收益对照和展开明细承载。
- 远航路线分支卡片必须提供回航结果短标：每条 `branchChoices` 必须从路线类型派生 `routeReturnKind` / `routeReturnText`，协同路线为 `far / 远航突破`，绕行路线为 `detour / 绕行突破`；主操作区默认折叠的 `far-dispatch-branch-choice-details` 必须写入 `is-return-far/is-return-detour`，summary 必须显示 `路线明细 · 远航突破/绕行突破`，并把回航结果短标语义纳入 `branchChoiceText` 与卡片标题。回航结果短标只改变展示，不新增收益、存档字段、远端指标或部署链路，完整语义继续由路线下一步、后续回航、收益对照、路线反馈和展开明细承载。
- 远航路线分支卡片必须提供路线指令串：每条 `branchChoices` 必须从当前目标指令和分支指令派生 `routeCommandLabels` / `routeCommandText`；主操作区分支卡片首屏必须渲染 `far-dispatch-branch-choice-route-command`，并用 `far-dispatch-branch-choice-route-command-step` 三个固定短槽显示目标指令、分支指令和回目标指令。路线指令串短槽必须复用 `routeStepLabels` 显示 1/2/3 步号，并用 `title` / `aria-label` 写入目标/分支/回目标、指令名和已完成/下一步/待推进状态；路线指令串必须纳入 `branchChoiceText` 与卡片标题；该展示不新增收益、存档字段、远端指标或部署链路，完整语义继续由路线预案、路线步骤、按钮标记、路线反馈和展开明细承载。
- 远航分支选择区必须提供路线对照条：存在 `branchChoices` 时，必须从现有分支卡片字段派生 `branchChoiceSummaryText`，并在协同/绕行卡片上方渲染 `far-dispatch-branch-choice-summary`；每个 `far-dispatch-branch-choice-summary-item` 必须显示路线徽标、当前步短标 `routePhaseText`、下一步按钮短标 `routeActionText`、收益短标 `routeRewardSummaryText`、第二步短标 `routeBranchStepText`、本步收益短标 `routePayoffSummaryText`、路线取向、当前资源代价和回航结果，并用资源图形区分协同保当前与绕行推累计。对照条必须把协同的 `0/3 起手 / 下一步 1 点火齐射 / 校准/补给/闭环 / 2 谐振脉冲 / 本步 +13% / 保当前 / 无消耗 / 远航突破` 和绕行的 `0/3 起手 / 下一步 1 点火齐射 / 校准/投送/闭环 / 2 巡航回收 / 本步 +4% / 推累计 / 消耗当前 / 绕行突破` 并排展示；完整语义继续由 `branchChoiceText`、卡片标题、路线微图、路线指令串、路线判断和展开明细承载；该展示不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供下一步按钮短标：每条 `branchChoices` 必须从现有 `routeNodeStates`、分支态势、本轮路线和 `routeCommandLabels` 派生 `routeActionKind` / `routeActionText`，覆盖 `下一步 1 <目标指令>`、`下一步 2 <分支指令>`、`下一步 3 <目标指令>`、`下一步 整备 <分支指令>`、`下一步 绕行整备 <分支指令>` 和待选；主操作区必须渲染 `far-dispatch-branch-choice-summary-action is-*`，并把短标纳入 `branchChoiceSummaryText`。该短标只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供下一步动作信标：`far-dispatch-branch-choice-summary-action` 必须以固定胶囊样式承载下一步短标；非 `is-idle` 状态必须通过 `::before` 播放 `farDispatchSummaryActionBeacon`，目标/分支/回航、整备和绕行动作可用不同强调色；`is-idle` 必须隐藏信标，避免待选路线被误读为当前动作。该信标只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前路线边栏：每个 `far-dispatch-branch-choice-summary-item` 必须从现有 `choice.active` 派生 `is-active-route`，当前路线必须通过边框、轻量背景和 `::before` 左侧边栏强调；该边栏不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前路线推进脉冲：仅当 `far-dispatch-branch-choice-summary-item.is-active-route` 时，迷你三步进度条填充段可以播放 `farDispatchSummaryActiveRouteSweep`，下一步节点可以播放 `farDispatchSummaryNextNodePulse`；必须提供 `prefers-reduced-motion: reduce` 关闭动画。该脉冲不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前动作亮环：仅当 `far-dispatch-branch-choice-summary-item.is-active-route` 且 `far-dispatch-branch-choice-summary-action` 非 `is-idle` 时，下一步动作胶囊可以播放 `farDispatchSummaryActiveActionGlow` 并获得亮环背景；必须提供 `prefers-reduced-motion: reduce` 关闭动画。该亮环不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前收益亮点：仅当 `far-dispatch-branch-choice-summary-item.is-active-route` 时，`far-dispatch-branch-choice-summary-payoff` 可以播放 `farDispatchSummaryActivePayoffGlow` 并获得胶囊边框与轻量背景；必须提供 `prefers-reduced-motion: reduce` 关闭动画。该亮点不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前资源代价锚点：仅当 `far-dispatch-branch-choice-summary-item.is-active-route` 时，`far-dispatch-branch-choice-summary-cost` 必须获得胶囊边框、轻量背景和内描边；`far-dispatch-branch-choice-summary-cost.is-spend` 必须提供消耗态颜色，突出绕行路线的当前资源代价。该锚点不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前回航结果锚点：仅当 `far-dispatch-branch-choice-summary-item.is-active-route` 时，`far-dispatch-branch-choice-summary-result` 必须获得胶囊边框、轻量背景和内描边，让当前路线的远航突破/绕行突破结果与当前动作、本步收益保持同层扫视；该锚点不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前阶段锚点：仅当 `far-dispatch-branch-choice-summary-item.is-active-route` 时，`far-dispatch-branch-choice-summary-phase` 必须获得胶囊边框、轻量背景和内描边，让当前路线的起手、分支、回航、完成或整备阶段与当前动作、本步收益保持同层扫视；该锚点不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前取向锚点：`far-dispatch-branch-choice-summary-intent` 必须从现有 `routeIntentKind` 派生 `is-preserve` / `is-advance` 状态类；仅当 `far-dispatch-branch-choice-summary-item.is-active-route` 时，取向短标必须获得胶囊边框、轻量背景和内描边，且 `is-advance` 必须使用单独高亮色突出推累计路线。该锚点不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前收益路径锚点：仅当 `far-dispatch-branch-choice-summary-item.is-active-route` 时，`far-dispatch-branch-choice-summary-reward` 必须获得胶囊边框、轻量背景和内描边；当当前路线同时为 `.is-detour` 时必须提供单独颜色，突出 `校准/投送/闭环` 的绕行收益路径。该锚点不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前第二步按钮锚点：仅当 `far-dispatch-branch-choice-summary-item.is-active-route` 时，`far-dispatch-branch-choice-summary-step` 必须获得胶囊边框、轻量背景和内描边；当当前路线同时为 `.is-detour` 时必须提供单独颜色，突出绕行路线的 `2 巡航回收` 分支按钮。该锚点不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前路线身份锚点：仅当 `far-dispatch-branch-choice-summary-item.is-active-route` 时，首行 `strong` 身份短标必须获得胶囊边框、轻量背景和内描边；当当前路线同时为 `.is-detour` 时必须提供单独颜色，突出当前绕行路线身份。该锚点不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前资源符号锚点：`far-dispatch-branch-choice-summary-glyph` 必须从现有 `routeResourceKind` 派生 `is-current` / `is-progress` 状态；仅当 `far-dispatch-branch-choice-summary-item.is-active-route` 时，当前资源符号必须获得更强光效和尺度强调，`is-current` 突出保留当前资源，`is-progress` 突出投送累计航段。`choice.routeResourceText` 必须进入对照槽 `title`；该锚点不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供非当前路线降噪：当 `far-dispatch-branch-choice-summary` 中存在 `.is-active-route` 时，非当前 `.far-dispatch-branch-choice-summary-item:not(.is-active-route)` 必须轻度降低视觉权重，并在悬停时恢复可读性。该降噪不得隐藏备选路线，不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前路线置顶：`renderFarDispatchBranchChoiceSummary` 必须在渲染前复制 `branchChoices` 并按 `choice.active` 优先排序，让本轮已选协同/绕行路线先显示；没有当前路线时必须保持原有协同/绕行顺序。该排序只改变对照条展示顺序，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前路线宽栏：当 `far-dispatch-branch-choice-summary` 中存在 `.is-active-route` 时，桌面端对照条必须切换为单列宽栏，让置顶后的当前路线获得完整横向空间，备选路线保留在下方并继续可读。该宽栏不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前路线操作收益宽位：当对照槽存在 `.is-active-route` 时，当前路线内部命名网格必须让下一步动作、本步收益和回航结果获得更宽槽位；备选路线保持原对照布局。该宽位不得新增可见文字，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供当前路线小屏宽位：在 `@media (max-width: 820px)` 下，`.far-dispatch-branch-choice-summary-item.is-active-route` 必须显式覆盖桌面 active-route 宽位，使用 18px 图形列加 3 个弹性文本列，并让下一步动作和本步收益各跨两列。该覆盖不得隐藏短标，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供迷你三步进度条：每个 `far-dispatch-branch-choice-summary-item` 必须渲染 `far-dispatch-branch-choice-summary-progress`，并从现有 `routeProgressPercent`、`routeNodeStates`、`routeStepLabels` 和 `routeCommandLabels` 派生 1/2/3 三点状态，覆盖已完成、下一步和待推进；该进度条必须显示 1/2/3 步号，并用 `role="img"`、`aria-label` 和 `title` 暴露“路线对照进度：1 目标 · 指令名 · 状态 -> 2 分支 · 指令名 · 状态 -> 3 回目标 · 指令名 · 状态”；完整阶段语义继续由 `branchChoiceSummaryText`、当前步短标、路线微图、路线步骤、按钮路线标记和路线反馈承载；该展示不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供收益短标：每条 `branchChoices` 必须从现有 `routeRewardLabels` 派生 `routeRewardSummaryText`，覆盖协同 `校准/补给/闭环` 和绕行 `校准/投送/闭环`；主操作区必须渲染 `far-dispatch-branch-choice-summary-reward`，并把短标纳入 `branchChoiceSummaryText` 与 `branchChoiceText`。该短标只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供本步收益短标：每条 `branchChoices` 必须从现有远航分支奖励常量、航段契合、路线稳航和分支改道判断派生 `routePayoffSummaryText`，覆盖协同首推 `本步 +13%`、绕行建档 `本步 +4%` 和稳航/改道/契合叠加后的即时收益；主操作区必须渲染 `far-dispatch-branch-choice-summary-payoff`，并把短标纳入 `branchChoiceSummaryText` 与 `branchChoiceText`。该短标只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须使用分组布局：`far-dispatch-branch-choice-summary-item` 必须使用命名网格区域，把路线身份、当前阶段、下一步动作、第二步按钮、收益落点、本步收益、资源取向、资源代价、回航结果和 1/2/3 迷你进度轨分层展示，避免所有短标挤在单行。该布局只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航分支选择区路线对照条必须提供小屏布局：在 `@media (max-width: 820px)` 下，`far-dispatch-branch-choice-summary` 必须改为单列，`far-dispatch-branch-choice-summary-item` 必须改用 18px 图形列加 3 个弹性文本列，并通过命名网格区域保留路线身份、当前阶段、下一步动作、第二步按钮、收益落点、本步收益、资源取向、资源代价、回航结果和 1/2/3 迷你进度轨。该布局不得隐藏短标，不新增收益、存档字段、远端指标或部署链路。
- 远航路线微图必须提供图例：存在 `branchChoices` 时，主操作区分支选择条必须在两张协同/绕行卡片上方渲染 `far-dispatch-branch-choice-legend`，用 `far-dispatch-branch-choice-legend-item` 显示 `1 目标`、`2 分支`、`3 回目标`；图例必须跨越两列并保持单行紧凑展示，`aria-hidden="true"`，语义继续由路线步骤、路线下一步、按钮路线标记、路线反馈和 `branchChoiceText` 承载；该图例只改变展示，不新增收益、存档字段、远端指标或部署链路。
- 远航分支卡片必须默认折叠路线明细：`far-dispatch-branch-choice` 首屏必须保留首行、路线微图、路线指令串、路线判断和推荐原因；`choice.objectiveText`、`choice.followupText`、`choice.nextText`、`choice.payoffText` 和资源/奖励说明必须放入默认关闭的 `far-dispatch-branch-choice-details`。完整语义必须继续保留在 `branchChoiceText`、卡片标题和展开内容中；该折叠只改变展示密度，不新增收益、存档字段、远端指标或部署链路。
- 远航调度必须提供“航段契合”：active 状态下由当前未完成航段奖励类型派生 `branchFocusKind` / `branchFocusText` / `branchFocusReasonText` / `branchFocusDirectiveId` / `branchFocusDirectiveName`；点击/过载奖励航段契合协同路线并显示“推荐原因：点击/过载航段保留当前资源”，自动/总产能奖励航段契合绕行路线并显示“推荐原因：自动/总产能航段投送累计航段”；目标指令后的契合分支必须额外获得有效基础指令收益 5%。分支选择条必须高亮契合路线并显示推荐原因与“航段契合 +5%”，远航路径收益、按钮徽标、预计收益、执行反馈、本地 `directive` 事件和反馈快照必须记录 `dispatchBranchFocusReward` / `dispatchBranchFocusRewardRate` / `dispatchBranchFocusRewardText`。航段契合不得新增存档字段，不得改变升级价格、星图航段顺序、项目完成判定、项目奖励、航线策略、指令基础收益、基础连携倍率、远航调度原有收益、冷却、连携窗口、反馈入口或部署链路。
- 远航调度必须提供“契合闭环”：active 状态下如果本轮使用当前航段契合分支，并回到当前航段目标指令完成 3/3，必须额外获得有效基础指令收益 7%。该判断只允许复用现有目标指令、分支选择、航段契合和指令链状态，不新增存档字段；按钮徽标、预计收益、执行反馈、本地 `directive` 事件、反馈快照和远航路径第三步收益标签必须记录 `dispatchFocusLoopReward` / `dispatchFocusLoopRewardRate` / `dispatchFocusLoopRewardText`；未走契合分支时不得触发。
- 远航调度必须提供“推荐分支”展示：active 状态下从 `branchChoices` 中选出当前航段契合路线，派生 `branchRecommendationText`，并合并推荐路线、指令、路线状态、资源取向、推荐原因、后续回航预告、航段契合 +5%；如果推荐路线状态为上轮路线，必须同时显示路线稳航 +4%；如果推荐路线状态为可改道，必须同时显示分支改道 +6%。主操作区必须渲染 `far-dispatch-branch-recommendation`，星图总览远航调度摘要必须包含推荐分支和推荐原因。该展示只从现有分支选择状态派生，不新增存档字段，不改变收益、冷却、连携窗口、星图航段、项目奖励、反馈入口或部署链路。
- 远航调度必须提供“分支轮替”展示：active 状态下从 `branchChoices` 的上轮路线、当前路线、路线稳航、可改道状态和 `farRouteBranchRotationDirectiveId` 派生 `branchRotationText`；没有上轮分支时提示先完成任一分支闭环，已有上轮分支时提示续走上轮协同/绕行路线触发“路线稳航 +4%”，或改走另一条路线触发“分支改道 +6%”并回到目标闭环触发“轮替闭环 +9%”，当前已续走上轮路线时提示已触发路线稳航，当前已走轮替分支时提示回到目标闭环结算轮替闭环，当前已走某路线但还没有上轮记录时提示完成闭环后下一轮可改道。主操作区必须渲染 `far-dispatch-branch-rotation`，星图总览远航调度摘要和反馈快照长文本必须包含该轮替目标。
- 远航调度必须提供“路线履历”展示：active 状态下从 `branchChoices`、当前分支态势和既有分支记录派生 `branchRouteText`，显示已记录路线、本轮路线和下一步确认；无记录路线时必须显示先完成任一分支闭环，当前已走协同/绕行时必须显示本轮路线，完成态必须提示下一轮可改道。主操作区必须渲染 `far-dispatch-branch-route`，星图总览远航调度摘要和反馈快照长文本必须包含路线履历。该展示只从现有状态派生，不新增存档字段，不改变收益、冷却、连携窗口、星图航段、项目奖励、反馈入口或部署链路。
- 远航调度必须提供“路线预案”展示：active 状态下从当前目标指令、`branchChoices`、分支态势和航段契合推荐派生 `branchPlanText`，显示推荐、本轮或下一轮“目标 -> 协同/绕行 -> 回目标”路线；待选择时优先使用航段契合推荐，协同/绕行进行中显示本轮路线，整备完成态显示下一轮可改道路线。主操作区必须渲染 `far-dispatch-branch-plan`，星图总览远航调度摘要和反馈快照长文本必须包含路线预案。该展示只从现有状态派生，不新增存档字段，不改变收益、冷却、连携窗口、星图航段、项目奖励、反馈入口或部署链路。
- 远航调度必须提供“路线步骤”展示：active 状态下从当前分支态势和 `branchChoices` 派生 `branchPlanStepText`，显示当前应执行的路线步；目标未起手时显示第 1/3 执行目标，目标后待分支时显示第 2/3 选择推荐协同/绕行及可选另一分支，协同/绕行进行中显示第 3/3 回航目标，整备态显示整备续航或整备回航。主操作区必须渲染 `far-dispatch-branch-step`，星图总览远航调度摘要和反馈快照长文本必须包含路线步骤。该展示只从现有状态派生，不新增存档字段，不改变收益、冷却、连携窗口、星图航段、项目奖励、反馈入口或部署链路。
- 远航调度必须提供“闭环复盘”展示：active 状态下从协同整备/绕行整备态派生 `branchClosureText`；刚完成 3/3 闭环时显示刚完成的协同/绕行路线、已触发的闭环与突破收益以及下一步整备，整备指令执行后显示该路线已整备并提示回目标触发整备回航。主操作区必须渲染 `far-dispatch-branch-closure`，星图总览远航调度摘要和反馈快照长文本必须包含闭环复盘。该展示只从现有分支态势、航段契合和指令链派生，不新增存档字段，不改变收益、冷却、连携窗口、星图航段、项目奖励、反馈入口或部署链路。
- 远航调度必须提供“路线按钮标记”：航线指令选项必须从当前分支态势派生 `dispatchRouteStepText`，并在按钮徽标中显示“路线 1/3 目标”“路线 2/3 推荐协同/绕行”“路线 3/3 协同回航/绕行回航”或“路线 整备续航/绕行整备/整备回航”；执行后行动反馈必须显示“路线执行：...”，本地 `directive` 事件必须记录 `dispatchRouteStepText`。该标记只从现有调度、分支选择和指令链派生，不新增收益、存档字段或部署链路。
- 远航调度必须提供“路线反馈”：航线指令执行结果必须从执行前的远航分支态势派生 `dispatchRouteResultText`，并在行动反馈中确认刚完成的路线结果和下一步，例如完成目标后提示选择推荐协同或绕行，执行协同/绕行后提示回目标，回航后提示整备，整备后提示回目标，整备回航后提示下一轮重新选择协同或绕行；本地 `directive` 事件必须记录 `dispatchRouteResultText`。该反馈不得新增收益、存档字段或部署链路。
- 远航调度必须把推荐分支落到按钮层：目标指令后的远航续航阶段，如果推荐按钮对应 `branchFocusDirectiveId`，按钮推荐徽标必须显示“推荐协同”或“推荐绕行”，冷却时显示“等待推荐协同/等待推荐绕行”；非契合分支保留“远航协同/远航绕行”。该展示只调整推荐文案，不新增收益、存档字段或部署链路。
- 远航调度条必须提供 3 步主路径轨：active 状态下由 `getFarRouteDispatch.loopSteps` / `loopStepText` 派生“目标 / 协同或绕行 / 回目标”三格，并按闭环进度标记下一步、已完成和待推进；锁定态和全部航段完成态不得显示空路径。路径轨只改变主操作区展示，不新增存档字段，不改变远航调度收益、冷却、连携窗口、星图航段、项目奖励或反馈入口。
- 20M 后当前星图航段卡片必须显示远航调度可见性：卡片头部显示“调度 X”徽标，卡片详情显示目标指令、协同续航指令、绕行指令名和 3/3 闭环提示，并用 3 步路径轨展示“目标 -> 协同 X / 绕行 Y -> 回目标闭环”；该展示只从当前航段、航线策略和现有远航调度映射派生，不新增存档字段，不改变远航调度收益、冷却、连携窗口、项目完成判定、星图航段顺序、项目奖励或反馈入口。
- 20M 后远航调度路径必须显示步骤收益标签：目标步标出调度校准，协同/绕行步标出远航协同、协同补给、绕行备选和绕行投送消耗，回目标步标出远航闭环、远航突破、绕行突破和轮替闭环；该展示只从现有远航调度奖励常量派生，不改变指令收益、冷却、连携窗口、远航突破数值、项目奖励、星图航段顺序或反馈入口。
- 20M 后星图总览必须显示远航调度总览：active 状态下显示当前航段、目标指令、协同续航指令、闭环进度和下一步路径；锁定态、未解锁态和全部航段完成态默认隐藏该行，避免首屏新增无效文字。该展示只从 `getFarRouteDispatch` 派生，不新增存档字段，不改变远航调度收益、冷却、连携窗口、星图航段、项目奖励或反馈入口。
- 航线指令完整 3/3 轮换后必须累积短期“指令熟练”：每层持续 3 分钟，让后续指令收益 +5%，最多 3 层；再次完成 3/3 轮换叠层或刷新持续时间；旧存档必须自动补齐 `directiveMastery`；预计收益、执行反馈、按钮徽标、本地 `directive` 事件和反馈快照必须记录熟练状态；3/3 完成态下若熟练层有效，推荐按钮必须显示“熟练续航”，无熟练层但仍处于满轮时显示“满轮续航”，冷却中显示“等待续航”；指令熟练满层 3/3 后继续完成 3/3 轮换必须触发“满层回响”，额外获得有效基础指令收益 10%，推荐按钮显示“回响续航”，冷却中显示“等待回响”，本地 `directive` 事件必须记录 `masteryCapstoneReward` 和 `masteryCapstoneRate`。指令熟练不得改变升级价格、星图航段顺序、项目完成判定、项目奖励、指令冷却、连携窗口、轮换目标奖励、策略契合 +10%、策略终结奖励数值或筛选规则。
- 星图航线策略后必须提供策略专精航段：点火航校强化点击路线，巡航航校强化自动路线，均衡校准强化总产能路线。
- 航线策略按钮在解锁后必须显示对应专精航段进度或完成状态，让策略选择和可追目标直接关联。
- 星图总览必须按当前航线策略显示对应专精航段、奖励和进度，让策略选择和中后段目标在同一信息层级可见。
- 星图计划必须提供总览，显示已完成项目数、总项目数、下一段名称、下一段奖励、进度详情、目标分轨、阶段导航、章节目标、章节奖励、奖励进度、奖励目标、里程碑、航线焦点、航线构成、奖励分布、已生效星图加成和最多 3 个未完成项目的短摘要预告；完整三段名称与奖励预告必须保留到 `title` / `aria-label`；目标分轨必须同时显示下一个累计航段和下一个升级航段；阶段导航必须显示首段星图、专精校准、深空基建、远航长尾四个阶段的完成数、当前阶段、本阶段剩余航段数和下一阶段名称；章节目标必须显示四个章节各自下一段未完成目标，章节完成后显示“已完成”；章节奖励必须按四个章节汇总总产能、点击、自动和过载奖励数量；奖励进度必须按总产能、点击、自动和过载显示已生效奖励数/总奖励数；奖励目标必须按总产能、点击、自动和过载显示下一条未完成奖励航段、奖励内容和当前进度；里程碑必须显示当前章节终点和终局航点的航段序号、奖励与进度，全部完成后显示全部章节已完成；航线焦点必须按当前航线策略显示对应专精航段、奖励和进度；航线构成必须显示累计/升级航段数量和四类奖励分布；全部完成时必须显示完成状态。
- 星图总览必须提供当前航段视觉卡：由第一个未完成项目状态派生航段号、章节位置、累计/升级推进类型、奖励方向和进度条，并以固定尺寸视觉槽显示在默认可见区域；全部航段完成时隐藏该卡。视觉卡只改变展示，不新增存档字段，不改变项目完成判定、奖励数值、航段顺序、筛选规则、航线策略或指令玩法。
- 星图总览必须提供四类奖励罗盘：由现有项目状态派生总产能、点击、自动和过载奖励的完成数、进度、下一条目标和当前/待推进状态，并以固定尺寸视觉槽显示在默认可见区域；罗盘只改变展示，不新增存档字段，不改变项目完成判定、奖励数值、航段顺序、筛选规则、航线策略或指令玩法。
- 星图总览必须提供航线预告视觉带：由现有未完成项目状态派生最多三条后续航段，显示航段号、章节位置、推进类型、奖励方向和进度条；默认可见的航线预告文本必须压缩为“接下来 N 段 · 下一段 X/Y 项目名”短摘要，完整三段名称与奖励说明必须保留到 `title` / `aria-label`。视觉带和短摘要只改变展示，不新增存档字段，不改变项目完成判定、奖励数值、航段顺序、筛选规则、航线策略或指令玩法。
- 星图计划必须提供视觉航线，把全部航段显示为已完成、当前和待推进节点；节点高亮必须跟随当前筛选命中范围，且只改变展示，不改变存档、项目完成判定、奖励数值、筛选规则或航段顺序。
- 星图计划必须包含项目内视觉资产，优先使用随静态构建发布的本地 SVG/图片；资产用于表现星核、航线节点和远航星门，不能依赖远端图片或改变存档、数值、航段顺序、筛选规则、航线策略或指令玩法。
- 星图总览的高密度明细必须默认折叠：主视图只保留星图进度、当前进度、行动建议和航线预告短摘要，目标分轨、阶段导航、章节目标、章节奖励、奖励进度、奖励目标、里程碑、航线焦点、航线构成和生效加成必须放入可展开的“星图明细”；折叠和短摘要只影响展示，不改变存档、数值、航段顺序、筛选规则、航线策略或指令玩法。
- 星图总览必须显示行动建议：升级航段复用升级可购买/能量缺口文案，累计航段显示剩余能量，当前自动产能大于 0 时显示预计时间；行动建议只影响展示，不改变存档、航段顺序、奖励数值和升级价格。
- 星图项目列表必须显示每个项目所属章节、章节内位置、推进方式标签、奖励类型标签和状态标签；状态标签必须区分“已完成”“当前航段”和“待推进”，并且只有第一个未完成项目显示为“当前航段”；当前航段必须保持说明、进度、奖励和进度条展开，非当前航段必须默认折叠这些详情；全部完成时不得显示当前航段。
- 星图项目卡片必须显示固定尺寸图标，分别覆盖推进方式（累计航段、升级航段）和奖励方向（总产能、点击、自动、过载）；图标只改变展示，不改变存档、项目完成判定、奖励数值、筛选规则、航线策略或指令玩法。
- 星图项目卡片必须显示固定尺寸可视化进度缩略图：由 `project.progress` 派生 `--project-card-progress`，并按累计/升级推进、完成/当前状态和总产能/点击/自动/过载奖励方向切换视觉形态；缩略图只改变展示，不新增存档字段，不改变项目完成判定、奖励数值、筛选规则、航线策略或指令玩法。
- 星图项目列表必须提供全部、当前、本章、累计、升级、总产能、点击、自动、过载、未完成和已完成筛选；默认初始筛选必须是本章，只显示当前航段所属章节，玩家仍可切换“全部”查看完整 57 段；本章筛选全部完成时为空；累计/升级筛选按项目是否依赖累计能量或升级等级归类；奖励类型筛选只显示对应成长线的航段；全部、本章、累计、升级和奖励类型等内容筛选按钮必须显示已完成/总数，当前、未完成和已完成筛选按钮必须显示直接数量；筛选区默认必须只显示一行短摘要，包含当前筛选名、完成数、下一条全局航段和剩余航段数，下一条默认文案不得重复章节内位置，完整筛选说明必须保留在 `title` / `aria-label` 和展开详情中；完整筛选详情必须默认折叠，并显示当前视图的航段数、完成数、完成率、剩余航段数、状态构成、奖励构成、推进构成、章节构成、已领取奖励、待领取奖励、下一条可推进航段、后续最多两条未完成航段和筛选终点；下一条、后续和终点航段必须同时显示全局航段号与章节内位置；已领取奖励只在当前视图同时存在已完成和未完成航段时显示，避免和已完成筛选的奖励构成重复；章节构成按首段星图、专精校准、深空基建、远航长尾顺序统计当前筛选视图中的命中航段；筛选只改变列表展示，不改变存档、项目完成判定、奖励数值或航段顺序。
- 星图筛选按钮必须提供固定视觉标识：全部、当前、章节、累计/升级、总产能/点击/自动/过载奖励、未完成和已完成入口应使用不同形状或颜色的小标记；标识只从筛选 id 派生，不新增存档字段，不改变筛选结果、星图航段、项目完成判定、奖励数值、航线策略、航线指令或反馈入口。
- 星图项目列表当前筛选结果超过 8 段时，必须默认只显示包含当前航段的 8 段窗口，并把其余航段收进默认关闭的项目列表抽屉；抽屉 summary 必须显示已收起段数和当前显示范围。该收起窗口只改变展示密度，不改变筛选结果、存档、项目完成判定、奖励数值、章节归属或航段顺序。
- 星图项目列表必须提供首段星图、专精校准、深空基建和远航长尾四个章节筛选入口；章节筛选按项目 `chapterName` 过滤，按钮显示已完成/总数，筛选摘要、筛选详情和星图视觉高亮必须复用现有筛选逻辑。章节筛选只改变列表展示，不改变存档、项目完成判定、奖励数值、航段顺序、航线策略或指令玩法。
- 星图视觉航线必须提供章节视觉导航：四个章节以固定尺寸可点击视觉按钮显示章节完成进度、下一条目标、小型进度轨、不同章节徽记和焦点短标签；点击后必须切换到对应章节筛选，并复用现有筛选摘要、项目列表和星图高亮逻辑。章节视觉导航只改变展示与导航，不改变存档、项目完成判定、奖励数值、航段顺序、航线策略、航线指令或反馈入口。
- 章节视觉导航按钮必须提供章节微图景：每个章节按钮内显示固定轨道、星门/信标和章节信号图形，并按首段星图、专精校准、深空基建、远航长尾切换视觉差异；微图景只改变展示，不新增存档字段，不改变章节筛选、项目完成判定、奖励数值、航段顺序、航线策略、航线指令或反馈入口。
- 星图视觉航线必须提供当前章节大图景：由现有章节视觉数据派生当前筛选章节或当前航段所属章节，显示更大的轨道、星门/信标、章节信号、章节名、进度和下一条目标；点击章节导航或切换章节筛选时必须同步更新，非章节筛选时回到当前航段所在章节。大图景只改变展示，不新增存档字段，不改变筛选结果、项目完成判定、奖励数值、航段顺序、航线策略、航线指令或反馈入口。
- 当前章节大图景必须提供章节节点带：由当前章节内项目状态派生已完成、当前、部分完成和待推进节点；短章节直接显示章节内项目节点，远航长尾等长章节必须压缩到最多 8 个节点。节点带只改变展示，不新增存档字段，不改变筛选结果、项目完成判定、奖励数值、航段顺序、航线策略、航线指令或反馈入口。
- 250K 以后星图计划必须继续提供至少一个长尾项目目标，避免首批项目完成后只剩循环累计目标。
- 500K 以后星图计划必须继续提供至少一个可见后续项目，避免远星中继完成后立即回到循环累计目标。
- 现有深空航段完成后必须继续提供 1.5M 后续目标，避免恒星锚点后立即回到循环累计目标。
- 星门远征完成后必须继续提供 2M 后续目标，避免 14 段路线完成后立即暴露内容边界。
- 外环信标完成后必须继续提供 2.75M 后续目标，避免 15 段路线完成后立即暴露内容边界。
- 彗尾船坞完成后必须继续提供 3.5M 后续目标，避免 16 段路线完成后立即暴露内容边界。
- 引力航标完成后必须继续提供 4.5M 后续目标，避免 17 段路线完成后立即暴露内容边界。
- 暗流观测站完成后必须继续提供 5.75M 后续目标，避免 18 段路线完成后立即暴露内容边界。
- 虚空罗盘完成后必须继续提供 7.25M 后续目标，避免 19 段路线完成后立即暴露内容边界。
- 星云档案库完成后必须继续提供 9M 后续目标，避免 20 段路线完成后立即暴露内容边界。
- 深穹测距阵完成后必须继续提供 11M 后续目标，避免 21 段路线完成后立即暴露内容边界。
- 寂光中继环完成后必须继续提供 13.5M 后续目标，避免 22 段路线完成后立即暴露内容边界。
- 极昼折跃井完成后必须继续提供 16.5M 后续目标，避免 23 段路线完成后立即暴露内容边界。
- 日冕回廊完成后必须继续提供 20M 后续目标，避免 24 段路线完成后立即暴露内容边界。
- 磁帆穹顶完成后必须继续提供 24M 后续目标，避免 25 段路线完成后立即暴露内容边界。
- 引潮星环完成后必须继续提供 30M 后续目标，避免 26 段路线完成后立即暴露内容边界。
- 脉冲航闸完成后必须继续提供 38M 后续目标，避免 27 段路线完成后立即暴露内容边界。
- 离辉轨道港完成后必须继续提供 50M 后续目标，避免 28 段路线完成后立即暴露内容边界。
- 远曦灯塔完成后必须继续提供 65M 后续目标，避免 29 段路线完成后立即暴露内容边界。
- 星冠回声站完成后必须继续提供 85M 后续目标，避免 30 段路线完成后立即暴露内容边界。
- 晨渊观星台完成后必须继续提供 110M 后续目标，避免 31 段路线完成后立即暴露内容边界。
- 曜幕天穹完成后必须继续提供 145M 后续目标，避免 32 段路线完成后立即暴露内容边界。
- 星渊回声门完成后必须继续提供 190M 后续目标，避免 33 段路线完成后立即暴露内容边界。
- 虚曜谐振塔完成后必须继续提供 250M 后续目标，避免 34 段路线完成后立即暴露内容边界。
- 烁影星匣完成后必须继续提供 320M 后续目标，避免 35 段路线完成后立即暴露内容边界。
- 辉潮巡航环完成后必须继续提供 420M 后续目标，避免 36 段路线完成后立即暴露内容边界。
- 星澜汇流庭完成后必须继续提供 550M 后续目标，避免 37 段路线完成后立即暴露内容边界。
- 穹海棱镜完成后必须继续提供 720M 后续目标，避免 38 段路线完成后立即暴露内容边界。
- 极光谱站完成后必须继续提供 950M 后续目标，避免 39 段路线完成后立即暴露内容边界。
- 银翼曙环完成后必须继续提供 1.2B 后续目标，避免 40 段路线完成后立即暴露内容边界。
- 晨星方舟完成后必须继续提供 1.6B 后续目标，避免 41 段路线完成后立即暴露内容边界。
- 星潮谐振庭完成后必须继续提供 2.1B 后续目标，避免 42 段路线完成后立即暴露内容边界。
- 星辉远港完成后必须继续提供 2.8B 后续目标，避免 43 段路线完成后立即暴露内容边界。
- 辉翼巡航港完成后必须继续提供 3.7B 后续目标，避免 44 段路线完成后立即暴露内容边界。
- 星穹导航塔完成后必须继续提供 4.9B 后续目标，避免 45 段路线完成后立即暴露内容边界。
- 星瀑谐振门完成后必须继续提供 6.5B 后续目标，避免 46 段路线完成后立即暴露内容边界。
- 星焰引航环完成后必须继续提供 8.6B 后续目标，避免 47 段路线完成后立即暴露内容边界。
- 星冕远航港完成后必须继续提供 11.4B 后续目标，避免 48 段路线完成后立即暴露内容边界。
- 曜冕星门完成后必须继续提供 15.2B 后续目标，避免 49 段路线完成后立即暴露内容边界。
- 曜潮谐振门完成后必须继续提供 20.4B 后续目标，避免 50 段路线完成后立即暴露内容边界。
- 星耀跃迁塔完成后必须继续提供 27.5B 后续目标，避免 51 段路线完成后立即暴露内容边界。
- 星翼远航港完成后必须继续提供 37.2B 后续目标，避免 52 段路线完成后立即暴露内容边界。
- 曜翼星门完成后必须继续提供 50B 后续目标，避免 53 段路线完成后立即暴露内容边界。
- 曜渊谐振门完成后必须继续提供 67.5B 后续目标，避免 54 段路线完成后立即暴露内容边界。
- 星渊点火塔完成后必须继续提供 91B 后续目标，避免 55 段路线完成后立即暴露内容边界。
- 星渊巡航环完成后必须继续提供 122B 后续目标，避免 56 段路线完成后立即暴露内容边界。
- 自动产能支持最多 8 小时离线结算；达到展示阈值时提示本次回收能量。
- 升级卡片必须直接显示购买进度、剩余能量或可购买状态，避免玩家在早期循环中依赖心算。
- 完成当前目标时必须在主操作区提示已完成目标和下一目标，避免目标切换被玩家错过。
